{
  "name": "pablo-project",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "project:status": "node scripts/project.mjs status",
    "project:context": "node scripts/project.mjs context",
    "project:check": "node scripts/project.mjs check",
    "test": "node --test scripts/project.test.mjs",
    "typecheck": "tsc --noEmit",
    "test:acp": "cargo build --locked -p pablo && node --test tests/acp.test.ts",
    "smoke:live:acp": "cargo build --locked -p pablo && node scripts/smoke-live-acp.ts",
    "test:telemetry": "cargo build --locked -p pablo && node --test tests/telemetry.test.ts",
    "smoke:collector": "cargo build --locked -p pablo && node scripts/smoke-collector.ts",
    "measure": "cargo build --release --locked -p pablo --bin pablo --example measure && node scripts/measure.ts",
    "measure:search": "cargo build --release --locked -p pablo --bin pablo && node scripts/measure-search.ts",
    "test:deployment": "cargo build --locked -p pablo --bin pablo --example measure && node --test tests/deployment.test.ts tests/deployment-telemetry.test.ts tests/shell-policy.test.ts tests/provider-selection.test.ts tests/openrouter.test.ts tests/open-responses.test.ts tests/model-routes.test.ts tests/model-fallback.test.ts",
    "bench:knowledge": "node scripts/knowledge-work/run.mjs",
    "test:knowledge": "node --test --test-concurrency=1 scripts/knowledge-work/benchmark.test.mjs",
    "test:install": "node --test scripts/install.test.mjs"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "1.4.0"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "ajv": "8.20.0",
    "typescript": "7.0.2"
  }
}
