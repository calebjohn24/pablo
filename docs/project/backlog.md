# Deferred work

This is an inventory of deferred work, not a second task-status system. Promote work into a cycle specification and the state file only when selected. [The design brief](../context.md), section 29.1, remains authoritative for the full 0.1 release.

[Cycle C3](cycles/003-extensibility-and-release.md) selects declarative deployment configuration, ordered model fallback, basic bounded-run context compaction, shell command allow/deny rules, OpenRouter, native Open Responses, stdio/Streamable HTTP MCP tools, local Skills, structured-output validation/repair, temporary children, minimal A2A client delegation, basic TUI, diagnostics, compatibility, packaging, measurements and prerelease distribution. Its dependencies preserve the promotion order: configuration precedes the features it configures; extensibility acceptance precedes interoperability, then release hardening. These capabilities are planned; [state](state.json) alone records implementation progress. Otto integration is excluded by the user's C3 instruction.

| Slice | Capability | Promotion condition |
| --- | --- | --- |
| Otto integration | Bounded read-only analysis through the freshly built binary and TypeScript ACP client on supported platforms | Explicitly selected after the C3 integration foundations have evidence. It remains a section 29.1/31.1 gate; synthetic C3 acceptance is not a substitute. |
| Full release designation | Complete beta.1/0.1 acceptance, including Otto and any still-unsupported required hard-limit profiles | All applicable design-brief release gates have actual evidence or the user deliberately revises the release contract. C3 prerelease publication alone cannot satisfy this. |
| Durable product work | Resumable root sessions, persistent children, storage/recovery, broader MCP, Python helpers, Graphline | The 0.1 cut line is satisfied and a new cycle is justified by host needs. |
| Optional breadth | AG-UI, Vercel AI SDK streams, OpenAPI import, advanced graphs/replay, cache policy, additional OTel signals | Native contracts survive dogfooding; each add-on or extension has its own small acceptance path. |
| Provider ceiling profiles | Evidence-backed live per-call token/cost upper bounds for hard aggregate budget admission | A provider/profile exposes enforceable bounds; C2.4 proves the ledger with attested offline fixtures and rejects unsupported live ceiling configurations. Reported usage alone is insufficient. |
| Policy recovery and breadth | Model continuation after policy denial, a broader shell language, environment forwarding and process/network controls | C3 selects exact command/argument rules but retains terminal policy denial and host-owned containment. Broader behavior requires its own evidenced contract. |
| Advanced deployment and routing | Nix language evaluation, remote module/package management, OS provisioning, active-run hot reload, adaptive model ranking and general retry orchestration | C3's bounded declarative presets and explicit ordered fallback pass real deployment fixtures, and a host selects the additional complexity. |

The former single-agent completion slice is complete in [cycle C2](cycles/002-single-agent-completion.md): filesystem operations, task/JSON output and the selected policy/accounting contracts. C2.5 acceptance passes on macOS arm64 and [native Linux x86_64](evidence/c2.5-linux-x64.md), including matched release measurements. This does not claim alpha.1 release completion. C3's output validation is separate from the existing task envelope; its namespace review must preserve generic ACP fallback and test explicit version migrations.

Release sizing remains an observation to address before ratifying distribution budgets: the Linux x86_64 C2 executable is 12.150 MiB versus 11.886 MiB for C1.7, both above the provisional 10 MiB target. Hosted-runner timings are mixed and do not establish universal performance equivalence. Preserve the [measured methods and limits](evidence/c2.5-linux-x64.md#matched-release-observations); no provisional number is a CI failure threshold.

Windows distribution, a general graph scheduler, interactive shell PTYs, adaptive provider routing, automatic Skill activation/installation, external ACP children and A2A serving remain outside C3. Ordered model fallback is explicitly selected by the user's deployment-config request. The TUI's real PTY acceptance does not add interactive shell tools. Preserve these boundaries and the broader MCP/Skill/telemetry rationale in the brief instead of scaffolding placeholder implementations.

C3.39 matched release measurements must revisit the [C3.9 filesystem timing increase](evidence/c3.9.md#matched-performance): repeated configured Vercel listing/replacement medians rose by fractions of a millisecond despite unchanged filesystem implementation. Preserve saved binaries and matched workloads; isolate runtime/lifecycle/host effects before claiming performance equivalence. This is a measured follow-up within the selected release checkpoint, not a new slice or an invented latency ceiling.

The user explicitly selected basic compaction for C3 on 2026-09-10. C3.12a supplies the already-required section 23/28.8 bounded-run behavior; durable summaries across resumed sessions, persistent memory/retrieval, tokenizer bundles and advanced context policies remain deferred. [Scope and Codex inspiration](evidence/c3-compaction-scope.md).

C3.39 must also revisit [C3.11 repeated filesystem comparisons](evidence/c3.11.md#performance): search/edit tool medians rose in both matched pairs (search by 0.085–0.334 ms; edit by 0.022–0.062 ms), while read/list declined and replacement varied. The large initial warm-route p95 did not repeat in alternating follow-ups. Retain measured source/binary/harness identities and isolate causes before claiming universal equivalence; these observations introduce no latency ceiling.
