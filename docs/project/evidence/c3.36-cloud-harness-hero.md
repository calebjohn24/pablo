# C3.36 cloud harness hero copy

Date: 2026-09-15

## Change

The primary landing headline now reads “A 15× smaller harness for your agents.” Its subheader identifies Pablo as a lightweight Rust harness for non-coding knowledge work. The adjacent comparison panel and the detailed performance section use “runtime” and “host-side” language so the positioning fits application-owned cloud deployments while the measured macOS environment remains explicit.

The 15× headline is anchored by the displayed Claude Code comparison: Pablo's 19.11 MiB peak process-tree memory and 0.37% median CPU utilization are approximately one fifteenth of the corresponding 286.09 MiB and 5.59% observations. The graphic continues to show Codex and Pi rather than extending that ratio to every compared harness.

## Verification

- `cd website && npm test` passed all eight build, source-sync, link, content, asset and release-route tests.
- Browser-use rendered the primary hero at 1728 × 1117 with separate copy and resource columns and no horizontal overflow.
- Browser-use rendered the hero at 390 × 844. The headline and subheader remained left aligned, the action buttons fit the viewport, the resource panel stayed within 390 pixels, and document width equaled viewport width.

Screenshots remain local ignored artifacts under `.pablo/tmp/`.
