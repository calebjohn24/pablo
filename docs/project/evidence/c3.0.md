# C3.0 cycle adoption evidence

Completed 2026-09-09 UTC. Baseline: `fb7cbb0141aed5f4a3befc91d4b1fb4dd22e01f1`, with a clean working tree at entry. This is a documentation/state checkpoint; no runtime behavior, credentialed integration or release gate is claimed.

## Selected plan

- [Cycle C3](../cycles/003-extensibility-and-release.md) defines 41 small checkpoints: this adoption plus 40 pending contracts, implementation, proof and delivery checkpoints. It includes declarative deployment configuration, shell command policy, both added provider paths, ordered model fallback, output validation/repair, both MCP transports, Skills, temporary children/handoffs, A2A client, basic TUI, diagnostics, compatibility, four native targets, measurements and prerelease distribution.
- The [scope/contract map](../contracts/c3-extensibility-and-release.md) identifies current C2 capabilities, shared invariants and the owner of each exact contract. The [fixture map](../fixtures/c3-extensibility-and-release.md) defines 37 unique fixture families covering every later checkpoint; all future behavior fixtures remain unexecuted specifications.
- The [deployment design](../contracts/c3-deployment-config.md) selects typed TOML, local modules/imports, named profiles, deterministic composition, validate/explain/render, locked deployment inputs and secret references. Every feature must enter the same option inventory. Ordered model fallback has one retry owner, per-attempt accounting and no tool replay; restrictive shell rules match executable/arguments and reject unsupported syntax. The proposed TOML example is labelled as future syntax.
- D029–D032 explain scope, small-checkpoint cadence, the unchanged full-release boundary and the user-selected deployment/fallback/command additions. Otto remains excluded from C3 and required by the unchanged brief; unsupported hard-spending profiles and provisional performance ceilings stay explicit. Publication follows preparation of concrete accepted artifacts and a separately selected action.
- README/brain/backlog now point to the selected plan and distinguish it from the existing C2 runtime. State remains version 1 and retains all 15 C1/C2 checkpoint objects. The prior 47 log entries and historical plans are preserved byte-for-byte.

## Verification actually performed

| Check | Result and scope |
| --- | --- |
| Entry `git status --short`, `node scripts/project.mjs context`, `node scripts/project.mjs check` | Passed: clean baseline, 15 completed checkpoints, 47 log entries, no active or selected next checkpoint |
| Project consistency after activating C3.0 and after the complete draft | Passed: only C3.0 active; all future work pending; headings/dependencies/state references consistent |
| `npm test` | Passed: 34 existing helper tests, zero failures/skips; rerun against the expanded configuration plan; no tests added for prose |
| `node --input-type=module` C3 adoption audit | Passed: identical prior checkpoint objects, source-context object/hash and historical plans; intact 47-entry log prefix; 41 contiguous C3 IDs; plan/state prerequisite parity; dependency order; 37 unique fixture families covering all 40 later checkpoints; local links, whitespace, brain limit and selected-context rendering |
| `python3` with standard-library `tomllib` | Passed: proposed deployment example parses; ordered model list, locked mode, shell argument rules and external credential references retain their specified shape; no configuration/runtime implementation claim |
| `git diff --check` and final document/diff review | Passed: reviewed selected scope, fallback versus retry/repair boundaries, command policy versus containment, prerequisite order, deferred Otto gate, four native targets and release claims; new files separately included in link/whitespace audit |

The adoption audit uses existing Node built-ins (`assert`, `fs`, `path`, `crypto`, `child_process`) and the helper's `inspectProject`/`renderContext`. It compares history with `git show` at the baseline, follows each dependency, matches fixture ownership to checkpoints, and validates local Markdown targets/anchors including the new documents. The final audit passed after completion records: 86 local links, preserved history, 56 total checkpoints/48 logs and C3.1 correctly selected. Installed Node is v24.11.0; the existing `.nvmrc` pin and source-context review object remain unchanged.

Reviewed C1/C2 cycle specifications, the C2 proposal/adoption evidence, current project records, provider/gateway/contracts/policy source and relevant design sections including 19–20, 29.1 and 31.1. Official protocol/configuration planning references and their use are recorded in the linked contract documents; implementation owners must pin audited revisions before use. The reviewed design SHA-256 remains `fc24bebe8f42a6b6d7ab66807b874dbaf95d20ef1602c1f6ed21fcb82fe6875f`.

Rust builds/runtime tests, paid provider calls, actual config loading/fallback, MCP/Skills/child/A2A/TUI execution, native acceptance, Collector runs, measurements and publication were not run for C3.0. They belong to their pending checkpoints. No credentials, runtime traces or build artifacts were inspected or added.

## Handoff

Complete C3.0 with LOG-0048; leave `active_checkpoint` null and C3.1 selected. The helper reports cumulative progress: 16/56 completed checkpoints, with all 40 later C3 checkpoints pending and empty verification arrays. No C3.0 blocker remains.

Next session: run the standard status/context/check commands, activate C3.1, and freeze the exact deployment option schema, composition/precedence/lock semantics and G01 examples from the deployment design. Finish that contract checkpoint and its records before implementing the loader at C3.2. Preserve all historical records and the one-checkpoint-per-session cadence.
