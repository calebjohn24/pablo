# C3.34 — README release preparation

The user requested a README update to prepare for release. This session covers that documentation slice of C3.34; archive/install implementation and B01 acceptance remain incomplete. The earlier hold on packaging execution remains in place. No later checkpoint was started.

## Changes

Replaced the historical cycle-oriented introduction with the current product, implemented capabilities and explicit prerelease status. Added a clone/build/offline-demo quickstart, gateway credential setup, everyday CLI examples, deployment inspection, ACP/Rust integration, diagnostics and trace guidance. Linked the implemented MCP, Skills, children, A2A, structured-output, compaction and reasoning contracts instead of describing those features as future work.

The README preserves host-owned sandbox/approval responsibility, shell write authority, fresh task context, credential precedence, optional call limits and unsupported hard live accounting ceilings. It distinguishes source builds from pending archives, installation, four-platform native acceptance, matched measurements and distribution. No proposed license, signing guarantee, platform floor or unaccepted release claim was added.

## Verification

- `cargo build --release --locked -p pablo --bin pablo` passed with the pinned local toolchain.
- A one-off Node verification checked all 30 local README links, including heading anchors; all resolved.
- The release executable passed `--version`, `--help`, `demo`, a demo with a temporary native trace, offline `doctor` with an explicit synthetic credential file, and both README production-preset `config validate` / `config explain` examples. The check used a minimal subprocess environment and removed its temporary files. It did not read the root credential file or contact providers.
- Documented flags were present in executable help; provider defaults, configuration/credential behavior, output semantics and integration examples were reviewed against implementation and linked contracts.

## Handoff

The requested README update is complete. C3.34 is blocked by the standing packaging hold and its acceptance criteria remain unfinished. When packaging work resumes, implement B01 archives/manifests/notices and explicit-prefix install/replace/remove checks, establish minimum OS/libc and signing limits, and update the README with the verified installation path. C3.35 native macOS arm64 acceptance follows only after C3.34 passes. No native release gate, live provider acceptance or release publication was performed in this session.
