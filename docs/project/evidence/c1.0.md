# C1.0 verification

Recorded on 2026-09-05 UTC (2026-09-04 America/Los_Angeles).

## Result

The project-memory checkpoint is complete. The brain, authoritative state, append-only work log, full cycle specification, backlog, and working instructions are present. The helper provides read-only `status`, `context`, and `check` commands. C1.1 is the next checkpoint; runtime implementation has not started.

## Environment and commands

Validation ran locally on macOS with Node 24.20.0 and npm 11.19.0 from nvm. Commands below assume that Node version is on `PATH`; equivalent invocations using the installed executable's absolute path were used during setup.

| Check | Result |
| --- | --- |
| `node --test scripts/project.test.mjs` | 34 tests passed; zero failed, skipped, or cancelled. |
| `node --check scripts/project.mjs` and `node --check scripts/project.test.mjs` | Both files parse successfully. |
| `node scripts/project.mjs check` | State, log, document links, checkpoint dependencies, completion evidence, and context hash are consistent. |
| `node scripts/project.mjs status` and `node scripts/project.mjs context` | Commands render the current state and focused handoff; fixture tests also execute them from outside the repository. |
| `npm install --package-lock-only --ignore-scripts --offline --no-audit --no-fund` | Generated the lockfile offline with no external dependencies. |
| `git check-ignore .env .env.local .env.production .pablo/traces/run.jsonl target/debug/pablo node_modules/example` | All listed private/generated paths are ignored. |
| `shasum -a 256 docs/context.md` | Matches the pre-implementation SHA-256 below. |

The preserved context digest is `fc24bebe8f42a6b6d7ab66807b874dbaf95d20ef1602c1f6ed21fcb82fe6875f`.

## Behavior covered

- Ready, active, and blocked checkpoint handoffs; selected specification boundaries; the last three log entries; bounded context excerpts.
- Unknown/duplicate checkpoint IDs, unsupported statuses, missing and cyclic dependencies, multiple active checkpoints, stale pointers, and incomplete blocker records.
- Completion without evidence, missing evidence files, failed/unrun checks, and evidence associated with another checkpoint.
- Malformed JSON/JSONL, stale context digest, incorrect timestamps/log ordering, missing decision references, broken file/heading links, and an oversized brain.
- Rejection of environment-file references, outside paths, and outside symlinks. Tests use synthetic credential sentinels and verify they do not appear in output.
- Actual CLI success/error exit codes, invocation from another directory, and byte/mtime snapshots proving helper commands do not modify fixture files.

## Failure found and resolved

The first test run failed because macOS resolves temporary directory aliases through `/private`, while path validation compared a canonical root to a noncanonical candidate. CLI entry detection had the same alias mismatch. Normalize repository roots and compare real entrypoint paths. The subsequent full suite passed all 34 tests, including real CLI invocation from an aliased temporary path.

## Handoff and limits

Start C1.1 from [its specification](../cycles/001-first-spike.md#c11-small-runtime-foundation): introduce the Rust core and executable with the minimal streamed lifecycle and native telemetry. Stop after that checkpoint's verification and records.

No provider call or Collector test belongs to C1.0, and neither was run. The user's `.env` contents were not read. State validation checks record consistency, not the truth of substantive acceptance claims. Append-only log history is a working convention retained in Git; the helper does not attest to historical authenticity.
