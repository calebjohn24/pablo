# C3.33 compatibility verification — LOG-0162 progress snapshot

This historical snapshot is superseded by [K01 completion evidence](c3.33-k01.md).

The implementation now negotiates C3 `pablo/v2` / `pablo/task-v2` and task revision `c3.33`. Frozen C2 source/client/schema bytes come from `ba8b0ff62b3ccc40c8a6ae704b4450ead12040b3`. Legacy unconfigured success and policy refusal validate against the frozen schema; configured C3 requests fail migration before delivery. Generic peers receive standard ACP without project metadata. Custom methods have explicit v1 names; the registry records project-controlled URIs, dependencies, schemas, bounds, privacy and fallbacks.

Actual custom notifications in compaction, Skills and model fallback tests validate against the checked-in notification schema. Terminal task repair states exclude intermediate states and require output validation. Seeded bounded parser mutations cover configuration, Skills, JSON Schema, A2A, MCP and Open Responses; ACP byte mutation cases require a valid initialization control, bounded output and process settlement. An idle configured host opens no network descriptors/listeners, MCP child or trace before prompting.

## Observed checks

- `cargo test --locked --workspace`: first run failed an old c3.13 task-version expectation. Updated that assertion to c3.33; full confirmation passed **347 tests, 15 existing ignored**, no failures.
- Initial positive-control run exposed missing contextId in the A2A mutation seed. Corrected the seed; all three K01 core mutation tests pass in the full suite.
- `node --test --test-concurrency=1 tests/skills.test.ts tests/compaction.test.ts tests/model-fallback.test.ts tests/compatibility.test.ts tests/output-validation.test.ts tests/output-repair.test.ts`: **82 passed**, no skips or cancellations.
- `cargo clippy --locked --workspace --all-targets -- -D warnings`, `npm run typecheck`, `cargo fmt --all -- --check`, and `git diff --check`: passed.
- No root credential file read or live provider check. Fixtures used synthetic credentials.

## Remaining acceptance and handoff

C3.33 remains in progress. The operation matrix is an inventory, not yet a completed immutable-pin verifier. Finish upstream/SDK/schema/fixture identities for all seven surfaces and executable offline pin verification; review all listed operations against code, finish current documentation migration and ensure project-owned extension bounds/privacy constraints are adequately verified. Run full serial Node tests, independent conformance audits, release builds and release compatibility/G04 checks. Retain matched performance against the saved C3.32 binaries, with no concurrent work during samples. Preserve earlier C3.29 concurrency failures and review their compatibility relevance.

No C3.33 PR or merge yet. Update evidence and project records with actual remaining results, merge the verified PR head, prove merged tree parity, then stop the active goal at C3.33 as requested. Do not start C3.34. Preserve the original user stash and saved measurement baselines.
