# C3.34 v0.0.1 release identity

The user selected `v0.0.1` as Pablo's first release tag. Workspace Cargo metadata and both locked workspace package entries now use `0.0.1`. The release workflow defaults to `v0.0.1` and still rejects any tag that does not exactly match the workspace version. Installer help, README commands, public guides, site navigation, route fixtures, release fixtures and Collector version expectations use the same identity. Historical benchmark evidence retains the version of the binary actually measured.

Cargo metadata reported version `0.0.1` and license `MIT` for both workspace packages. Cargo formatting, clippy with warnings denied, all workspace tests, six release and installer tests, TypeScript type checking, six site tests, shell and Node syntax, project consistency and diff checks passed. A new four-target candidate must be built from the merged licensed commit before C3.34 can complete.
