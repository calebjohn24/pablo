# OpenRouter credential retest during C3.9

On 2026-09-10 the user requested a live test of the new OpenRouter key in the ignored root `.env`. `node scripts/smoke-live-openrouter.ts target/release/pablo .env` passed through both CLI and ACP using `z-ai/glm-5.3-flash`. The explicit credential-file argument bypassed ambient OpenRouter credentials; only the executable resolved the file. The key was not inspected, printed or copied into records.

| Surface | Elapsed ms | Model/tool calls | Input/output tokens | Cost, micro-USD |
| --- | ---: | --- | --- | ---: |
| CLI | 3,017.431 | 2 / 1 | 1,709 / 137 | 305 |
| ACP | 3,140.241 | 2 / 1 | 1,707 / 136 | 304 |

Each task read a fresh unseen marker through one allowed `fs.read`, returned its exact contents and produced one terminal outcome. ACP streamed the expected answer and tool result. Native accounting matched the task result; redaction, private trace permissions, normal process exit and workspace cleanup passed. Shell execution was disabled. Combined recorded cost was 609 micro-USD ($0.000609), using the adapter's rounding. These two samples are live functional checks, not a performance distribution.

The tested release executable has SHA-256 `00ca1d0236cd0a4d3d9500e7d48faa2fed240e280449bfcba136c91caa55a3a2`, the previously verified C3.6–C3.8 binary. This retest does not validate the uncommitted C3.9 implementation. The smoke harness also fingerprints the current workspace; that value must not be mistaken for the tested binary's build-source identity. Native macOS arm64, Node 24.11.0, Rust 1.98.1.

C3.9 remains in progress. Resume its Open Responses terminal/accounting cases, rebuild and run affected Rust/Node regressions, compare matched release performance, then complete documentation and acceptance records before merging. Existing implementation edits were preserved.
