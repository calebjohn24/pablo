# C2.4 policy and accounting

Verified on macOS arm64, 2026-09-05 UTC. All four host-owned policy dimensions are enforced: exact tools and executable launcher, component-based read/write roots. Deny precedes allow; first matching rule has stable identity, allowlist misses use reserved built-in IDs, invalid/duplicate IDs and unknown fields reject configuration. Capability absence still wins over an allow. CLI and ACP share the same host policy; explicit write/root denial cannot be overridden. Allowed dispatch IDs enter tool serialization before result-size admission (including precommit mutation admission) and appear in metadata-only spans. Executable rules constrain `/bin/sh` only, not its commands/descendants.

The run-local ledger uses optional u64 total-token and integer micro-USD ceilings. An adapter must attest a per-call bound for model/output-limit configuration; ordinary gateway usage is insufficient, so live/fixture HTTP gateway ceiling requests reject before delivery. A complete reserve is required before each dispatch; token failure has precedence over cost. Known actual input+output and adapter cost settle reservations, unknown actuals/uncertain delivery retain them, definitely-not-sent failures release to zero while retaining the attempted call. Actual aggregation uses checked arithmetic; bound violations, including overflowing token sums, stop immediately and retain reserves. No retry/fallback or hidden default count cap was added. Native revision c2.4 carries the settled task accounting; task envelope shape/version c2.3 remains compatible.

P01–P04: real filesystem tests prove deny precedence, exact wildcard nonmatching, missing-capability intersection, minimum-sized allowed result accounting, root component/first-deny tests and closed validation. CLI/ACP integration executes or prevents an actual shell marker under the same launcher allow/deny policy, validates rule identity and asserts no process on denial. Existing root/write-denial and private configuration fixtures remain passing.

A01–A06: attested provider integration fixtures execute six model/five filesystem calls with default unlimited counts, reserve 10 then settle 6 tokens against cap 15, reserve 8 then settle 3 cost against cap 10, retain unknown 10/8 charges, distinguish delivery certainty, preserve earlier known usage across later unsent failure, reject unsupported/zero/violated bounds, and cancel before or after dispatch without extra work. Fresh runs reuse the fixture provider with fresh ledger state; ACP independent session envelopes retain fresh counts. CLI/ACP HTTP fixture ceilings both reject with zero requests. Existing zero model/tool count and final-answer-hint tests remain passing. Integer-wire tests cover exact u64 and overflow-to-unknown totals.

Checks: 90 Rust workspace tests passed, none ignored; 80 ACP/telemetry tests passed, none skipped; TypeScript typecheck, clippy with warnings denied, formatting, whitespace and project checks passed. An initial bound-overflow fixture detected missing explicit overflow violation handling; corrected before the passing run. No live cost-bound claim is made.

Continue with C2.5 full acceptance, real Collector filesystem correlation, bounded live ACP filesystem task and matched C1.7 release measurements. Native Linux x86_64 access is unresolved; the existing Linux arm64 VM can add evidence but cannot fulfill that gate.
