# C3.33c — Pablo GLM rerun

At the user's request, reran only Pablo with OpenRouter `z-ai/glm-5.3-flash`, provider-default reasoning, using the current merged runtime. Six task families, seed 42, one attempt per task, 180-second timeout, no retries. The other five configurations were not invoked. This refreshes the original six-harness pilot; the separate 270-attempt reasoning comparison is unchanged.

[Updated graphic](c3.33c-glm-rerun.png) · [SVG](c3.33c-glm-rerun.svg) · [sanitized measurements](c3.33c-glm-rerun-measurements.json)

| Pablo GLM metric | Previous pilot | Rerun |
|---|---:|---:|
| Factual accuracy, higher better | 24/25 (96%) | 25/25 (100%) |
| Completed attempts | 6/6 | 6/6 |
| Wall p50 seconds, lower better | 25.97 | 22.18 |
| Wall p95 seconds, lower better | 101.07 | 31.36 |
| Peak tree memory MiB, lower better | 30.50 | 19.11 |
| CPU p50 seconds, lower better | 0.087 | 0.083 |
| CPU p50 percent of one core, lower better | 0.26% | 0.37% |
| Six-task reported cost USD, lower better | $0.015281 | $0.011403 |

The rerun's median was 14.6% lower. This small, later sample does not establish a causal performance improvement: model output, provider routing/cache state and network conditions vary. CPU utilization divides CPU time by wall time, so it can increase despite lower CPU seconds. Correct factual answers do not establish full prose quality; handoff and incident citation-set diagnostics were imperfect and remain in the JSON. No strict whole-task pass metric is shown.

All 30 non-Pablo-GLM observations are copied exactly from the historical comparison, including their model/effort settings and costs. Earlier Pablo rows remain in the new JSON as superseded observations, outside current aggregates. The original comparison JSON/PNG/SVG and all latency/transport artifacts are byte-for-byte unchanged. The new chart explicitly labels only Pablo GLM as rerun and retains costs, all six metric directions, and the distinction between reported costs and the historical Codex estimate.

## Verification and reproduction

Before measurement, `cargo build --release --locked -p pablo --bin pablo` passed and `node --test --test-concurrency=1 scripts/knowledge-work/benchmark.test.mjs` passed all 17 tests. The optimized binary hash is `b549d9eee623313cd26bf71712b48d2feff87006bfdc1011afe0f0e48c820fe2` (15,108,832 bytes), matching the earlier reasoning comparison. The build uses the unchanged default HTTP feature set. No build or test overlapped retained measurements.

```sh
node scripts/knowledge-work/run.mjs --live --harnesses pablo \
  --seeds 42 --repeats 1 --timeout 180 \
  --output .pablo/measurements/knowledge-pablo-glm-rerun-20260913

.pablo/knowledge-plot-venv/bin/python scripts/knowledge-work/plot.py \
  docs/project/evidence/c3.33c-glm-rerun-measurements.json \
  docs/project/evidence/c3.33c-glm-rerun
```

The runner refuses an existing output directory; use a new path for another run. The merged artifact replaces all six Pablo GLM rows, without selection by score, and retains the other 30 rows unchanged. It recomputes aggregates from current rows, excludes strict passes from headlines, preserves source/executable identities and archives superseded rows separately. Plotting now includes failed attempts rather than filtering them away; this does not change the retained other configurations, which all completed.

Audits confirmed six unique tasks, matching prior task prompts, current source fingerprints, unchanged source files, default reasoning on every call, known reported costs, matching source/executable hashes and exact preservation of the other 30 rows. A private selected-key scan covered 271 evidence/local-artifact files with zero matches. Raw streams and task workspaces remain ignored locally; published measurements omit commands, private paths, raw text and resource sample arrays. PNG/SVG rendering passed, and the PNG was visually inspected for labels, values, directions, spacing and caveats. Final project consistency and whitespace checks passed.

C3.33c is complete after this requested follow-up. C3.34 remains selected next but release packaging is explicitly held.
