# C2.0 contracts and scope evidence

Completed 2026-09-05 UTC. Baseline: `8b8cc690094173c5dcaaf9380052604715d05b02` (clean working tree at session start). This checkpoint adopts specifications and project records; it does not implement filesystem tools or claim any C2 runtime acceptance gate.

## Review and resulting scope

Reviewed the existing `contracts.rs`, `tool.rs`, `runtime.rs`, CLI entry point and runtime/shell/ACP/telemetry documentation against architecture sections 12.1/12.5/12.7/12.8, 14.3/14.4, 20.3, 29.1 and 30. The [gap table](../contracts/c2-single-agent.md#gap-review) separates observed C1 behavior from selected work and the full release contract. The design brief and its previously reviewed SHA-256 are unchanged: `fc24bebe8f42a6b6d7ab66807b874dbaf95d20ef1602c1f6ed21fcb82fe6875f`.

- [Cycle C2](../cycles/002-single-agent-completion.md) adopts all six proposed checkpoints with dependencies and substantive acceptance gates. Filesystem policy enforcement starts alongside tools, not after their implementation.
- [Contracts](../contracts/c2-single-agent.md) define arguments/results, UTF-8/page/work bounds, no-follow roots, recoverable errors, optimistic mutation preconditions, atomic visibility, cancellation, privacy, exact policy matching/rule IDs, task framing and conservative accounting admission/settlement.
- [Fixtures](../fixtures/c2-single-agent.md) freeze 31 synthetic acceptance cases: eight read cases, six mutation cases, three shared lifecycle cases, four JSON cases, four policy cases and six accounting cases. Explicit C2.5 gates cover native Linux x86_64, live filesystem use, real Collector export, compatibility and matched measurements.
- D021–D023 record why the scope, filesystem and accounting choices exist. The brain remains below its 200-line limit. The backlog distinguishes the promoted slice from deferred provider ceiling profiles, broader policy recovery and extensibility.
- General model JSON Schema output/repair remains alpha.2. The C2 task envelope preserves the native outcome and final string; it does not claim model structured-output validation. Live reported usage alone cannot support a hard pre-dispatch cost guarantee; an unsupported ceiling profile must reject before sending.
- Updated the instructions to resolve the selected cycle through state, the README/proposal/brain to point to adopted C2, and the stale runtime overview to acknowledge C1.5's already-implemented network telemetry export.

## Verification actually performed

| Check | Result / scope |
| --- | --- |
| `git status --short` at entry | Passed: clean tree; no existing user edits to preserve |
| `node scripts/project.mjs context` and `node scripts/project.mjs check` at entry | Passed: C1 had 9 completed checkpoints and 33 log entries; no active checkpoint |
| `node scripts/project.mjs check` after activating C2.0 and after drafting | Passed: 15 checkpoint records; C2.0 active, C2.1–C2.5 pending; no dangling C1 references |
| `npm test` | Passed: 34 existing helper tests, zero failures/skips; no tests added for documentation wording |
| `node --input-type=module` adoption audit | Passed: compared all nine C1 checkpoint objects against baseline, checked byte-identical 33-line log prefix, unchanged source-context object/hash, six new C2 records, active C2.0 context selection, 73 local links across nine changed/new Markdown files, 31 unique fixture IDs, exact sample UTF-8 sizes and brain limit |
| `git diff --check` and final diff review | Passed for tracked changes; the adoption audit separately checked new Markdown whitespace and links. Reviewed runtime claims, dependency order, policy/mutation boundaries and deferred gates |
| Final adoption audit and `node scripts/project.mjs check` after completion records | Passed: 12 documentation/record files, 77 local links, intact C1 history, 15 checkpoints/34 log entries, C2.0 done and C2.1 ready; all later verification arrays empty |

The adoption audit uses Node built-ins (`assert`, `fs`, `path`, `crypto`, `child_process`) plus the existing helper's `inspectProject`/`renderContext`. Historical comparisons use `git show` at the baseline above, object equality for C1 records and a byte-prefix comparison for the append-only log. Local Markdown targets and heading anchors are checked, including the new contracts/fixture documents. This verifies record preservation and fixture coverage, not future behavior.

Runtime Rust/ACP tests, builds, provider calls, Collector runs, platform acceptance and performance measurements were not run for this documentation/state checkpoint. The 31 fixture cases are specified and remain unexecuted; no future checkpoint verification reference is populated. No credentials, raw task data, ignored traces or build output were inspected or added.

## Handoff

Mark C2.0 done using LOG-0034; leave active_checkpoint null and C2.1 selected. Preserve all C1 checkpoints/evidence and append to the existing log. The helper's progress count is cumulative across retained cycles, so completion shows 10/15 overall; C2.1–C2.5 remain pending.

Next session: run the standard context/check commands, mark C2.1 in progress, then implement `fs.read`, `fs.list`, `fs.search` and their read-policy subset through the existing registry/provider/CLI/ACP lifecycle. Start with F01–F08 and L01–L03. Preserve C1.7's setup reuse, first-text delivery, redaction and measurement methods; stop after C2.1 verification and records. C2.0 has no unresolved acceptance blocker.
