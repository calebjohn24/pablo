# C3.10 OpenRouter credential retest

The user requested a live test of the new ignored root `.env` credential. `node scripts/smoke-live-openrouter.ts target/release/pablo .env` passed using `z-ai/glm-5.3-flash`. The explicit file reference bypassed ambient OpenRouter credentials; only Pablo resolved the key. The harness did not inspect or print the key.

CLI completed in 8.912 seconds; ACP completed in 10.058 seconds. Each task performed two model calls and one allowed `fs.read` of a fresh unseen marker, returned its exact contents, and produced one terminal outcome. Native accounting, streamed ACP output, trace redaction and permissions, normal exit and workspace cleanup passed. Shell was disabled. Combined reported cost was 307 micro-USD ($0.000307). Two live functional samples do not establish a performance distribution.

Release binary SHA-256: `467648bd345875487543d9b0787e9d9d3c7b9153fb855e49af61280598677ff0`; source fingerprint: `7f1fbc75ad9b3c9ba1445a75f935c155911c1639f9bc51727ad574d7a8c45c3e`. This checks the current C3.10 executable's existing OpenRouter path; it does not claim live multi-entry fallback acceptance. Raw runtime evidence remains ignored.
