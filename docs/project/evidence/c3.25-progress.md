# C3.25 combined acceptance progress

The shared independent E01 fixture now drives TypeScript ACP and direct Rust
`Runtime` embedding through the production root factory. Node supplies only the
synthetic provider/MCP/Skill fixture and verifies results for the Rust path; it does
not drive a root ACP session in that path. Both drivers pass completion, root
cancellation and denied-authority variants.

The root activates a Skill and reads its resource, uses a real independent Python
MCP tool, and starts two children. It completes one source while retaining an active
sibling, then receives a forced context overflow. Compaction removes completed tool
history while preserving the pinned Skill, root catalog, completed result ID and
active child handle. The resumed root re-inspects the completed result, uses MCP
again, waits for the sibling, and passes inline/artifact handoffs to a final child.
That child reads the artifact and computes schema-valid answer 42. Cancellation and
denied write-capability selection after compaction retain truthful child outcomes
and joined MCP cleanup. No runtime implementation change was required.

Focused ACP: three passed. Direct Rust embedding: one test covering all three
variants passed. Full Node suite: 280 passed. TypeScript and project tests (34),
formatting and diff checks passed. Full Rust/explicit gates and release builds
remain in progress; final performance and source-matched live evidence are pending.

Real Collector 0.160.0 proof passed with 37 spans: four runs, seventeen models,
eleven tools, one compaction and four output validations. Run/model/tool/compaction
identities, parents, agent/root attributes and timestamps match native events;
validation spans match their native terminal's run and schema digest. Export omits
private task, Skill/resource/evidence and credential markers. Stopping the Collector
leaves outcome and accounting unchanged in the same combined workload.

The initial Collector assertion incorrectly expected 33 spans, omitting the four
existing output-validation spans. The corrected proof explicitly checks those
spans' agent/run/schema attribution instead of discarding them. Initial Rust harness
borrow-scope compilation and TypeScript callback-return errors were corrected before
the passing focused runs. These were fixture issues, not runtime failures.
