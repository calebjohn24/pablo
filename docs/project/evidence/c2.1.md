# C2.1 bounded filesystem reads

Verified on native macOS arm64 on 2026-09-05 UTC. The user explicitly authorized continuing through the rest of C2 in this session, overriding the usual one-checkpoint stop. Existing uncommitted C2.0 specifications and records were preserved.

Implemented `fs.read`, `fs.list`, `fs.search` through the existing registry, gateway name mapping, CLI, ACP and native/OTel lifecycle. Workspace handles are opened at admission; bounded blocking workers use no-follow descriptor-relative traversal and are joined before tool settlement. UTF-8 snapshots, full-file SHA-256 revisions, stateless pages, sorted direct listings and recursive literal search follow the selected contracts. Result byte accounting includes serialization and counts retained search matches incrementally.

Read-root policies enforce exact subtree rules and deny precedence. Unsupported policy dimensions reject setup in this checkpoint. The native revision is `c2.1`; the extension schema/reference client handle the added filesystem limit and configured/symlink policy outcomes while retaining historical revision acceptance. CLI/ACP enable reads by default; both `--no-shell --no-filesystem` are needed for text-only execution. No mutation or accounting capability is advertised yet.

The existing pinned `rustix` dependency enables its filesystem feature; SHA-256 uses the already-locked AWS-LC 1.18.1 implementation. No upstream dependency versions changed. Filesystem payloads are boxed to avoid expanding every conversation/event by the largest read-result variant. Error-only boxing avoids carrying a full tool result through every successful helper return. Redacted serialization continues to borrow payload metadata without cloning content.

## Behavior evidence

| Frozen cases | Checked-in tests and observations |
| --- | --- |
| F01–F05 | `crates/pablo-core/tests/filesystem.rs`: actual content reaches the next model request, independently checked SHA-256, UTF-8 page boundaries/EOF, sorted listing pages, exact literal matching lines, binary/error handling and argument rejection |
| F06–F08 | Same integration file plus `filesystem::unix::tests`: outside/parent/internal-link denial, anchored root surviving path replacement, swapped intermediate link rejection, per-file/entry/depth/scan/result bounds, growth after initial read, FIFO rejection without blocking, unreadable-file error and cancellation during reading |
| L01/L02 | Core tests correlate exact native/span identities and timestamps and check default/captured content projections. `tests/acp.test.ts` verifies real gateway aliases, standard tool kinds, result content, schema-valid metadata and privacy |
| L03 | Existing disconnect/backpressure/cancellation/independent-session tests remain passing. A new two-session filesystem test verifies distinct workspaces/traces, no content-history leak and one reused HTTP connection |

APFS rejected creation of the invalid-UTF-8 filename with EILSEQ. The test verifies that platform behavior; Linux must exercise Pablo's own rejection of a successfully created invalid name at C2.5. The permission test requires an unprivileged runner and fails explicitly if chmod does not prevent reads. No Linux, live-provider, real-Collector or performance gate was claimed here.

## Verification

- `cargo test --workspace --locked`: 66 passed, no failures or ignored tests.
- `node --test tests/acp.test.ts tests/telemetry.test.ts`: 67 passed, no failures or skips.
- `npm run typecheck`: passed.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: passed after resolving findings.
- `cargo fmt --all -- --check`, `git diff --check`, `node scripts/project.mjs check`: passed.

Earlier checks exposed an unavailable macOS FIFO-at API in the fixture, APFS invalid-name refusal, old text-only fixture flags, an outdated TypeScript policy-outcome parser, large error/result types and lint findings. They were corrected and the checks above rerun. No acceptance failure remains for this checkpoint's macOS scope; cross-platform proof remains C2.5.

## Contract clarification and handoff

D024 clarifies that a requested filesystem result capacity must be at least 1,024 bytes, matching the existing host minimum. A smaller request cannot represent bounded failure metadata; it is rejected as an invalid argument before filesystem work. The 8 MiB default remains unchanged. Byte-limit detection may read one extra byte, with retained data still bounded.

Complete C2.1 and activate C2.2 under the user's full-cycle instruction. Implement revision-checked write/edit, atomic create/replace, explicit write opt-in, commit-aware cancellation and M01–M06 while retaining these read and transport regressions. Saved C1.7 release binaries are in ignored local storage for the final matched performance comparison. Native Linux x86_64 runner access was requested asynchronously and remains a C2.5 requirement.
