# C3.1 deployment configuration contract evidence

Completed 2026-09-09 UTC on `plan/c3-deployments`. Clean entry baseline: `77c3c4c2b7d637416d0ab5581071bb90761f4642`. This is a specification checkpoint; no deployment loader, config command, authority runtime integration or later feature is implemented or claimed by this change.

## Contract and artifacts

- The [contract](../contracts/c3-deployment-config.md) freezes TOML 1.0.0/schema version 1, the `options` envelope, defaults/import/profile/environment/explicit-override precedence, atomic named-definition conflicts, list replace/append/prepend/clear, explicit path bases, finite parser/import/profile/inspection work and deliberate version migration. Authority accumulates independently of ordinary values; locked deployments ignore ambient files and SDK environment inputs and allow only named per-task changes within their ceilings.
- The [option inventory](../contracts/c3-deployment-options.md) maps current CLI, `RunSpec`/`RunLimits`/filesystem/trace/policy, ACP and OTel settings to typed fields, exact defaults and implementation owners. It distinguishes serializable values, secret references, host callbacks/dynamic data and compiled safety/platform invariants. Later shell/provider/route/output/MCP/Skill/child/A2A/TUI/diagnostic/compatibility options have explicit owners and remain rejected until their slices implement them.
- Two closed Draft 2020-12 schemas describe [source documents](../schemas/deployment-v1.schema.json) and [resolved configurations](../schemas/resolved-deployment-v1.schema.json); [defaults](../schemas/deployment-defaults-v1.json) retain C2's current model, instructions, unlimited call counts, time/byte capacities and OTel defaults. Semantic constraints beyond JSON Schema are documented, not silently treated as validated by a shape check.
- The [G01 corpus](../fixtures/c3-deployment/README.md) includes base/development/production presets, separate environment/private-file/host credential-source examples, fourteen invalid TOML documents, three canonical byte/hash vectors, and a manually authored production result with 77 effective leaf/list-item provenance entries. Credential contents and task input never enter these artifacts. Named file references in credential examples intentionally have no corresponding secret files.
- Effective configuration identity is separate from source history and host path binding identity. Exact source digest payloads, deterministic source IDs, path normalization and canonical UTF-8/JSON escaping are frozen. The authored production config fingerprint is `sha256:05da699dbe1fad56d03edcd5ed1d0fbceea7932fda05907492187eff08c86c7e`; its input fingerprint is `sha256:f666c9503d6f2cada8ae91bc2c1c50b9c7415050913bce23ad60130ca35c7e84`. These verify contract vectors, not observed loader output.
- Thirty G02/G03 semantic cases specify actual future resolution, authority, secret isolation, CLI/ACP/Rust equivalence, render reload, compatibility and snapshot outcomes. Each remains `not_run`; their owning commands are documented as future suites. D033/D034 record the contract and identity decisions. README and the C3 contract/fixture maps now link the concrete artifacts.

## Verification actually performed

| Check | Observed result |
| --- | --- |
| Entry `git status --short`, `node scripts/project.mjs context`, `node scripts/project.mjs check` | Clean tree, C3.0 complete/C3.1 selected, 56 checkpoint records and 48 log entries; activated C3.1 before drafting |
| `npm test` | All 34 existing helper tests passed, zero failures/skips; no tests added for prose |
| `python3 docs/project/fixtures/c3-deployment/audit.py` | Passed two schema meta-validations, exact baseline default shape, 18 TOML shape cases (4 accepted, 14 rejected at their expected stages), 3 independent canonical byte/hash vectors and the resolved example's hashes, source digests and complete valid provenance references |
| `node --input-type=module` with installed AJV 2020 | Both schemas compiled in strict mode and the production example validated independently; no network schema retrieval |
| `node --input-type=module` Rust option inventory audit | Exact field-set coverage for current `RunLimits`, `FilesystemLimits`, `TraceSettings` and `Policy`; source-schema/default keys agree; CLI instruction bytes and fixed Vercel endpoint match current Rust source |
| `node --input-type=module` history/link/scope audit | Preserved the reviewed source-context object/hash, all other checkpoint objects, 48-entry log prefix, historical cycle specs, runtime, dependency/build inputs and user instructions; all local document targets/anchors and text whitespace valid; brain remains below 200 lines |
| First post-handoff `node scripts/project.mjs check` | Failed: LOG-0049 put a future-scope note in `problems`; the helper treats every nonempty problem array as unresolved. Reopened C3.1, preserved LOG-0049, and recorded the correction in LOG-0050 with an empty problem array |
| Corrected `node scripts/project.mjs check`, `git diff --check`, final diff/record review | Passed consistency, single-checkpoint cadence, schema/default/example ownership and the specification-only completion boundary; final handoff selects C3.2 |

Installed tools used: Node v24.11.0 (the existing `.nvmrc` remains pinned to 24.20.0), Python 3.12.13, Python `jsonschema` 4.26.0 / `referencing` 0.37.0 and repository AJV 8.20.0. No installation or dependency change was needed. The one-off Node audits use existing `assert`, `fs`, `path`, `crypto`, `child_process` and the project helper; compare against the baseline commit above and inspect modified/new files. The included Python auditor is strictly an artifact check, not a duplicate config resolver.

Review clarified ordinary versus locked override permissions, group enablement versus per-call policy constraints, exact source-manifest ordering/digest payloads and default provenance for empty policy lists before freezing the artifacts. All final audits pass; no unresolved C3.1 blocker remains. Official TOML 1.0.0 and JSON Schema 2020-12 sources were inspected, with links in the contract. The product brief and reviewed SHA-256 `fc24bebe8f42a6b6d7ab66807b874dbaf95d20ef1602c1f6ed21fcb82fe6875f` remain unchanged.

Rust builds/tests, executable config loading, G02/G03 behavioral enforcement, actual credential resolution, paid provider checks, Collector/PTY/native release tests and publication were not run for this documentation checkpoint. No runtime traces, credential values, real credential files or build artifacts were read or added.

## Handoff

LOG-0049 retains the work and passing contract checks; LOG-0050 corrects its administrative completion metadata without rewriting history and completes only C3.1. State records 17/56 checkpoints done, no active checkpoint, and C3.2 next. C3.2 must implement the bounded local loader and deterministic resolver against this contract and the G02 cases without starting an agent, resolving secrets or activating configured capabilities. Add meaningful resolver tests, preserve the exact production golden/merge expectations, and record any deliberate contract change with evidence before implementation. CLI/ACP/Rust integration and validate/explain/render remain C3.3; shell command rules and ordered routes remain their later checkpoints.

Start the next session with status/context/check, activate C3.2 and clear `next_checkpoint` before code changes. Keep one checkpoint per implementation session and stop after its verification/handoff. C3.1 makes no broader C3 or full-release completion claim.
