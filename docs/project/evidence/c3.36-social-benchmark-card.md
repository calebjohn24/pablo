# C3.36 social benchmark card

Date: 2026-09-15

## Artifact

`website/public/social/benchmark-15x.png` is a 1200×630 feed image for LinkedIn and X. Its editable source is `website/public/social/benchmark-15x.svg`; adjacent logo assets provide the orange Pablo, coral Claude and green Codex marks.

The lead claim is `15×+ MORE CPU`. It is tied specifically to median CPU utilization on the six-task benchmark: Pablo measured 0.37% of one core, Claude Code 5.59% or 15.1× Pablo, and Codex 6.04% or 16.3× Pablo. Peak process-tree memory is presented separately: Pablo 19.11 MiB, Codex 196.03 MiB or 10.3× Pablo, and Claude Code 286.09 MiB or 15.0× Pablo. The graphic retains the macOS arm64, six synthetic tasks, one seed/attempt and non-matched-route limitations in its footer.

## Verification

- `rsvg-convert --width 1200 --height 630 website/public/social/benchmark-15x.svg --output website/public/social/benchmark-15x.png` produced a 1200×630 RGB PNG with the three colored harness marks resolved into the raster.
- Original-resolution inspection confirmed that the headline, CPU values, ratio pills, memory strip, marks and URL fit without clipping or overlap.
- A 600×315 downsample inspection confirmed the headline, three harness identities, CPU bars, exact CPU values and multiplier pills remain legible at feed-preview size.
- `cd website && npm test` passed all eight site tests, including public asset dimensions, exact benchmark claims, logo references and their distinct colors.

After PR #60 merged, main Docs deployment `34995916390` passed. Downloading `https://runpablo.pages.dev/social/benchmark-15x.png` returned a 1200×630 RGB PNG whose SHA-256 `c6e105a5a6ed6044a516734d2458aa27f852a40630e122bbb17e19de45c9c032` exactly matched the reviewed repository artifact.

## Footprint message revision

The shareable card now leads only with `15× SMALLER FOOTPRINT`. The prior `MORE CPU`, same-workload sentence, utilization caption and bottom methodology line were removed. The lower strip now contains only the peak-memory label, colored harness marks, names, values and ratios; its extra Rust-harness tagline was also removed. Exact CPU values and ratios remain in the chart so the claim has visible quantitative support.

Both the original URL and the cache-busted `benchmark-15x-footprint.png` contain the corrected card. Original-resolution and 600×315 inspections confirmed the reduced message, values and logos remain readable without clipping. `cd website && npm test` passed all eight tests, including assertions that reject the removed copy and require identical PNGs at both paths.

Production verification for the revised asset follows merge and deployment.
