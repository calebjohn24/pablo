# C3.36 landing hero and heading-anchor alignment

## Scope

- Removed `measured footprint` from the landing-page hero label so it renders as `Pablo`.
- Matched the shared level-two permalink offset to the site's custom 8 px heading top padding.

## Verification

- `cd website && npm test` passed all 8 site tests after building every public page.
- Browser review at 1440 × 1000 confirmed the focused level-two permalink occupies the same 32 px line box as its heading text. Browser measurements reported 8 px CSS top offset and 9 px rendered offset from the heading border edge, accounting for the 1 px divider.
- Browser review at 390 × 844 confirmed the permalink aligns to the first line of a wrapped level-two heading. The landing hero exposes `Pablo` as its name, preserves the release title, and reports `scrollWidth` equal to `innerWidth` (390 px).
