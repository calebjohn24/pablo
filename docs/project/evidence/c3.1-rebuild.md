# C3.1 rebuild handoff

The user requested a rebuild for local testing after the specification checkpoint. Rebuilt the existing release executable on native macOS arm64 on 2026-09-09 UTC. No implementation checkpoint was started; C3.2 remains next. Existing uncommitted C3.1 work is preserved.

- Command: `cargo build --release --locked -p pablo`; passed using the existing incremental build.
- Executable: `target/release/pablo`, version `0.1.0-dev.1`, 10,464,624 bytes.
- SHA-256: `9cb6d1f2c04e8f2906bf05ab80638fe4798e565cefa89249c960840aef1b245c`.
- Source HEAD: `77c3c4c2b7d637416d0ab5581071bb90761f4642`; C3.1 changes are documentation/schema/fixture records, so the executable remains identical to the prior runtime build.
- `./target/release/pablo --version` passed.
- `OTEL_TRACES_EXPORTER=none ./target/release/pablo demo` passed and printed `Hello from pablo.` with exit 0.

Run from the repository directory:

```sh
./target/release/pablo run "Read README.md and summarize it." --no-shell
```

The runtime privately resolves the existing gateway credential when the user invokes a live task. This rebuild performed only an offline startup smoke; no live provider call, credential-file access or full-suite rerun was performed. C3 deployment files are specified but cannot be loaded until the later implementation checkpoints. Build output remains ignored.

LOG-0051 records this requested build follow-up. C3.1 remains complete, C3.2 remains pending/selected, and its loader/resolver handoff is unchanged.
