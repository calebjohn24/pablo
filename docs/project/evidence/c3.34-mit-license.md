# C3.34 MIT license

The user selected the MIT License for Pablo while the first four-target candidate run was compiling. That run was cancelled before any package or index step so an obsolete unlicensed source identity could not become a candidate. The root `LICENSE` now contains the MIT terms with 2026 Pablo contributors attribution. Workspace metadata declares `MIT`, and both `pablo` and `pablo-core` inherit it.

The archive packer includes the exact project LICENSE next to NOTICE, and the verifier requires it. NOTICE now points to the MIT license while keeping the generated target-specific third-party inventory distinct. README and public installation documentation identify the license, and the source fingerprint now covers LICENSE, NOTICE and the installer.

Cargo metadata reported MIT for both workspace packages. Six packaging/installer tests, six site tests, project consistency and diff checks passed after the change. The next candidate run must use the licensed commit; cancelled run 34885052662 is not evidence for an artifact.
