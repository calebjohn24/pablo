# C3.36 footprint-first landing hero

Date: 2026-09-15

## Change

The landing page now leads with Pablo's measured local resource footprint. Its primary hero names the 10–20× positioning range, then gives the exact comparison: Claude Code and Codex used 10–16× more local memory and CPU in the six-task run, Pi used about 8× more, and Pablo measured 19.11 MiB peak process-tree memory with 0.37% median CPU utilization.

A new terminal-style comparison graphic puts peak memory and median CPU bars for Pablo, Claude Code, Codex and Pi beside the primary copy. The previous “The agent runtime your application can own” hero, its embedder explanation, actions and terminal example now form a full-width secondary hero. The detailed responsive benchmark graphic remains on the landing page and in the benchmark guide with method and limitations.

## Verification

- `cd website && npm test` passed all eight build, source-sync, link, content, asset and release-route tests.
- Browser-use rendered the landing page at 1728 × 1117. The primary copy and comparison graphic occupied separate columns without overlap; the secondary hero began below a full-width divider. Document width equaled viewport width at 1728 pixels.
- Browser-use rendered the full page at 390 × 844. The primary copy, both action rows, secondary hero and terminal content were left aligned. The comparison panel fit within the viewport, the mobile benchmark asset was selected, and document width equaled viewport width at 390 pixels.

Screenshots are retained as local ignored artifacts under `.pablo/tmp/`.
