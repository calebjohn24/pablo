# C2.3 single-task machine output

Verified on macOS arm64, 2026-09-05 UTC. Shorthand delegates to the existing run parser. JSON output projects `TaskResult` from the native terminal event, with no event transcript. Native revision c2.3 adds terminal accounting; task schema c2.3 uses decimal-string u64 counters and nullable actual usage/cost/charges. No dispatch means known zero; unavailable dispatched usage and checked-add overflow remain unknown.

J01/J02: CLI executable fixtures prove exact 9007199254740993 input tokens, quotes/LF/Unicode/literal JSON as a string, one object plus LF, no progress output, identical native IDs/outcome/accounting, shorthand parity, and closed pre-admission errors with exit 2. J03: cancellation cleans up a real child and returns 130 with terminal accounting; provider failure and zero model limit return 1 and match native truth. Existing runtime/ACP failure, timeout, policy, event-delivery, output-limit and cancellation tests remain passing. The serializer tests escaping bounds before writes, rejects noncanonical/overflow decimal strings and stops on the first closed-writer error. J04/L03: optional pablo/task-v1 negotiation yields the shared envelope and independent session accounting; generic and existing pablo/v1-only tests retain their response shapes and transport bounds. Existing maximum escaping/event-reservation tests remain passing.

D026: negotiated terminal metadata substitutes `task` for `outcome`, avoiding a duplicate large output inside the 32 MiB ACP frame. Old peers retain their existing shape. Exact accounting is in decimal strings; legacy numeric usage remains compatible. The reference client validates through u64 and exposes strings for BigInt conversion. JSON output is bounded by six times output bytes plus 8 KiB metadata, checked without a second output buffer. Documentation and both schemas record this transition.

Checks: Rust workspace tests (80 passed, none ignored); ACP/telemetry tests (74 passed, none skipped); TypeScript typecheck; clippy all targets with warnings denied; format, diff and project consistency checks all passed. Cost/charge fields intentionally remain null until the C2.4 ledger; this is not a hard-ceiling implementation claim. Native Linux, live, Collector and matched performance gates remain C2.5.

Continue immediately with C2.4 under full-cycle authorization: complete exact tool/executable policy and trusted per-call reservations with checked settlement and all-outcome parity.
