# C3.36 landing layout and public copy review

## Scope

- Reversed the second landing-page section at desktop widths so the terminal demonstration leads on the left and the runtime copy sits on the right.
- Preserved copy-first reading order in the single-column mobile layout.
- Replaced public prerelease framing with current-version, installation, compatibility and support facts across the landing page, site footer, README and guides.
- Reworked the public release-status guide into a customer-facing Version and support page.

## Automated verification

- `cd website && npm test` passed all 9 site tests after building every public page.
- The public-source and generated-site scans found no `prerelease` or `pre-release` wording.
- `node scripts/project.mjs check` passed before recording this evidence.

## Browser review

- At 1440 × 1000, the landing page placed the terminal at x=164 and copy at x=782, had no horizontal overflow, broken images or empty links, and rendered the full page without collisions.
- At 390 × 844, landing copy remained before the terminal, the page stayed left aligned, and document width matched the viewport exactly.
- Reviewed `/getting-started`, `/installation`, `/embedding`, `/benchmarks` and `/release-status` at 1440 × 1000 and 390 × 844. Every route had matching viewport/document widths, an unclipped H1, no broken images and no removed status wording.
- Inspected full-page desktop and mobile captures for the landing page and Version and support page. Navigation, tables, code styling, content spacing, sidebars, outline and footer remained visually coherent.
