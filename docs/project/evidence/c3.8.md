# C3.8 acceptance — pinned Open Responses contract

OR01 is complete as a contract checkpoint. No Open Responses execution is enabled or claimed. The [contract](../contracts/c3-open-responses.md) freezes configuration/capability resolution, credential and endpoint scope, HTTP/SSE transitions, input/output/tool correlation, private continuation, finish/usage/errors, limits and cancellation before C3.9 implementation.

## Evidence

- Immutable upstream revision `92c12d96d7b61d6d15e2214daa5e9c6000ab6e1c`, release 2026-04-24. The [manifest](../fixtures/c3-open-responses/upstream-pin.json) records hashes/byte counts of unchanged released OpenAPI, specification, acceptance implementation, SSE parser and license. Sources were fetched from the upstream Git repository; the offline audit rechecks all retained bytes and local-only schema references.
- Three schema-valid requests, 46 schema-valid semantic events and matching CRLF SSE wires cover two tool rounds, ordered complete continuation, message phases, fragmented function arguments, multiple Unicode text parts, opaque reasoning and a streamed private summary. Six additional terminal examples cover success, length truncation, rejected unsupported incomplete reason, provider failure and null/zero usage.
- An independent consistency audit checks item/part sequencing, call/result correlation, final snapshot equality, repeated input prefix and exact continuation, private-state exclusion from public text and accounting. Ten malformed request/event shapes reject through the upstream schema; thirteen schema-valid lifecycle mutations plus missing DONE reject independently.
- The pinned output schema accepts raw reasoning content that its input schema cannot represent. An executable negative example proves this asymmetry; the contract explicitly rejects that unsupported form while preserving encrypted content and summaries. The normative summary-event discriminator contains `_text`; fixture names follow the schema rather than inconsistent descriptive wording.
- The proposed capability profile and deployment example remain explicitly future C3.9 configuration. Existing P01 still proves Open Responses cannot reach credentials, traces or fixture endpoints. No current schema revision/default was changed.

## Checks actually run

- `node --test docs/project/fixtures/c3-open-responses/audit.mjs`: **10 passed**, no skips; includes all 52 event examples and three requests.
- `node --test tests/provider-selection.test.ts`: **2 passed**, no skips.
- `npm test`: **34 passed**, no skips; `npm run typecheck`: passed.
- `python3 docs/project/fixtures/c3-deployment/audit.py`: passed existing G01 artifact checks; its printed G02/G03 mappings do not claim runtime execution.
- Production-source/dependency diff and release hash/size comparison: passed. Binary SHA-256 `00ca1d0236cd0a4d3d9500e7d48faa2fed240e280449bfcba136c91caa55a3a2`, 11,231,136 bytes, identical to C3.6/C3.7.
- `node scripts/project.mjs check` and `git diff --check`: passed.

No production runtime or dependency changed. The [C3.6 matched performance](c3.6-measurements.json) still measures this exact executable, and [C3.7](c3.7-live.json) retains its real OpenRouter timings. This contract audit is not a runtime benchmark or an adapter conformance result; C3.9 must rebuild and run matched measurements after implementation. No paid provider call was needed here.

Initial staged whitespace review flagged the wire fixtures' required CRLF/terminal blank separator and formatting in the unchanged upstream specification. Scoped Git attributes preserve those exact bytes and disable formatting checks only where protocol framing or upstream hashes require it; authored contract/audit code retains ordinary whitespace checks. The final staged check passed.

## Durable decision and handoff

D043 pins the protocol and keeps private continuation task-scoped because flattening output to chat text would discard required provider state and risk leaking it across reused ACP runs. The brain remains within 200 lines; the reviewed product-context hash is unchanged.

Merge C3.8, then activate C3.9. Implement the native configured Open Responses path, including the shared decoder's event-name retention, scoped private continuation carrier, explicit header credentials and closed unsupported-content errors. OR02 must execute real filesystem tool rounds through core/CLI/ACP, fragmented HTTP, failure/cancellation/privacy/usage cases and all affected gateway regressions. Compare the rebuilt release against the frozen C3.6/C3.7 binary and retain distributions. Do not substitute these contract fixtures for runtime execution proof.
